# 多平台发布系统：分步实施路线图 (Phase by Phase)

为了稳步推进项目开发，我们将任务划分为 **5 个核心阶段**。每个阶段都有明确的产出成果。

## 阶段 1：后端基础与数据库落地 (已启动)
**目标**：建立稳固的底层数据存取能力。
1.  **数据库初始化**：执行 `数据库设计脚本.sql`，建立 5 张核心表。
2.  **JPA 实体映射**：在 `java-work` 中完成 Account、Article、Platform 等 Entity 的编写（已完成部分）。
3.  **持久层开发**：完成所有 Spring Data JPA Repository，确保基础的 CRUD（增删改查）通过测试。

## 阶段 2：平台适配器 (Adapter) 核心设计
**目标**：解决不同平台 API 的差异化调用。
1.  **定义标准接口**：编写 `PlatformAdapter.java` 接口，定义上传媒体、发布文章、查询状态的统一标准。
2.  **实现百家号适配器**：作为第一个样板程序，实现百家号的 API 调用逻辑（内容校验 -> 图片转存 -> 文章投稿）。
3.  **实现企鹅号适配器**：对比百家号逻辑，优化适配器架构，使其能支持更多渠道。

## 阶段 3：异步分发调度引擎
**目标**：实现高可靠、异步的任务执行逻辑。
1.  **任务分发逻辑**：编写 Service 层，实现将文章拆解为多个子任务（PublishTask）并存入数据库。
2.  **消息队列集成**：使用 Redis 或 Spring 异步事件处理队列中的任务。
3.  **重试机制**：由于 API 可能出现临时波动，需设计指数退避重试逻辑。

## 阶段 4：前端 UI 基础与 API 对接
**目标**：搭建 Vue 3 + TS 交互界面。
1.  **基础框架搭建**：在 `vue_view` 中安装 Element Plus、Axios、Pinia、Sass 等依赖。
2.  **API 封装**：编写针对后端接口的 TypeScript 请求封装和类型定义。
3.  **核心页面开发**：
    - 账号绑定管理页（扫码/授权 UI）。
    - 统一稿件在线编辑器（集成图片上传）。
    - 实时分发进度大盘。

## 阶段 5：联调、测试与多媒体洗涤
**目标**：全链路打通。
1.  **图片转存逻辑优化**：实现后端自动解析文章 HTML，将外链图/本地图自动上传到各个平台的对应图床。
2.  **全链路冒烟测试**：实测一键发布一篇文章到 2 个不同的平台账号。
3.  **监控与错误处理**：完善发布失败时的错误提示（如：由于敏感词被平台拒绝）。

---

### 🚀 当前优先推荐：
我们目前处于 **阶段 1 与阶段 2 的交接处**。建议接下来的具体步骤为：
1.  **移动与规范化代码**（将之前生成的代码移动到 `java-work` 指定包下）。
2.  **完善 Repository 层并编写测试类**。
3.  **开始设计 `PlatformAdapter` 接口**。

请问您对这个步骤安排是否满意？或者是否有哪个阶段您希望优先进行？
